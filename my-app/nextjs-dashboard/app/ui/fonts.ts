import { Lato, Raleway } from 'next/font/google';

export const lato = Lato({
    weight: ['400', '700'],
    subsets: ['latin'] });

export const raleway = Raleway({
    weight: ['400', '700'],
    subsets: ['latin'],
});